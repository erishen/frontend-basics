// LeetCode 238 · 中等
// 除自身以外数组的乘积
// answer[i] = 除 nums[i] 外所有元素乘积（O(n)，不用除法）。
// TODO: 实现 productExceptSelf
// 提示：JS 里 `0 * 负数` 会得到 -0，而测试用 deepStrictEqual 区分 -0 与 0，
//      返回前用 res.map((v) => (v === 0 ? 0 : v)) 归一化即可。

function productExceptSelf(nums) {
  // TODO: 在这里实现
  throw new Error("TODO: 请实现");
}

module.exports = productExceptSelf;
