// 数组扁平化 flat
// 支持指定深度

/**
 * @param {any[]} arr - 嵌套数组
 * @param {number} depth - 扁平深度，默认 Infinity
 * @returns {any[]}
 */
function flat(arr, depth = Infinity) {
  // TODO: 在这里实现
}

module.exports = flat;
